"""Infrastructure layer.

Existing SQLAlchemy repositories, Redis adapters and messaging adapters live here.
incrementally. Current use cases depend on the old adapters through FastAPI
dependency wiring in the presentation layer.
"""
